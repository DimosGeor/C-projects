#include <stdio.h>
void decompose (long x);
main()
{
   long x;
   printf("Please insert non-negative number:")
   x=GetInteger();
   decompose (x,&c);
}

void decompose(long x);

{
    long temp,g;
    int c,i;
    temp=x;
    c=0;
    while (temp != 0)
    {

        g= temp % 10;
        temp=temp/10;
        if (d > 0 && x % g == 0)
              c++;
    }
    for(i=0;i<c;i++)
    {
        sum=
    }

}
